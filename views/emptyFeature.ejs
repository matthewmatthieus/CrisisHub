<div class="page-header">
    <div>
        <h1><%= pageTitle %></h1>
        <p><%= pageDescription %></p>
    </div>
</div>

<div class="content-card empty-feature-card">
    <div class="empty-feature-icon">
        <i class="<%= iconClass %>"></i>
    </div>

    <h2><%= emptyTitle %></h2>
    <p><%= emptyMessage %></p>
</div>
