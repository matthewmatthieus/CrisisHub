<div class="container-fluid py-4">

    <!-- Page Header -->
    <div class="page-header">

        <div>
            <h1>
                Help Requests
            </h1>

            <p>
                Review community requests for urgent assistance.
            </p>
        </div>

        <% if (isAdmin) { %>

        <div class="page-actions">

            <a class="btn btn-primary" href="/helpRequests/new">
                <i class="bi bi-plus-circle me-1"></i>
                New Request
            </a>

        </div>

        <% } %>

    </div>

    <!-- Data Table Card -->
    <div class="card bg-dark text-white border-secondary shadow rounded-3 overflow-hidden">
        <div class="card-header">

            <h2>
                <i class="bi bi-life-preserver"></i>
                Open Help Requests
            </h2>

            <span class="text-secondary">
                <%= requests.length %> requests
            </span>

        </div>

        <div class="card-body p-0">

            <% if (requests.length === 0) { %>

                <div class="text-center py-5">
                    <i class="bi bi-inbox display-4 text-muted"></i>
                    <p class="text-muted mt-3">
                        No help requests have been submitted yet.
                    </p>
                </div>

            <% } else { %>

                <div class="table-responsive">

                    <table class="table table-dark table-hover align-middle mb-0">

                        <thead style="--bs-table-bg:#2b3035;">
                            <tr>
                                <th class="px-3">ID</th>
                                <th>Title</th>
                                <th>Category</th>
                                <th>Urgency</th>
                                <th>Qty</th>
                                <th>Location</th>
                                <th>User ID</th>
                                <th class="text-end px-3">Actions</th>
                            </tr>
                        </thead>

                        <tbody>

                            <% requests.forEach(request => { %>

                                <tr>

                                    <td>#<%= request.id %></td>

                                    <td class="fw-bold">
                                        <%= request.title %>
                                    </td>

                                    <td>
                                        <span class="badge border border-info text-info">
                                            <%= request.category %>
                                        </span>
                                    </td>

                                    <td>
                                        <% if (request.urgency === 'Critical') { %>

                                            <span class="badge bg-danger">
                                                <%= request.urgency %>
                                            </span>

                                        <% } else if (request.urgency === 'High') { %>

                                            <span class="badge bg-warning text-dark">
                                                <%= request.urgency %>
                                            </span>

                                        <% } else { %>

                                            <span class="badge bg-secondary">
                                                <%= request.urgency %>
                                            </span>

                                        <% } %>
                                    </td>

                                    <td>
                                        <%= request.quantity_needed %>
                                    </td>

                                    <td>
                                        <%= request.location %>
                                    </td>

                                    <td class="text-muted">
                                        User #<%= request.user_id %>
                                    </td>

                                    <td class="text-end">

                                        <div class="btn-group">

                                            <a
                                                href="/helpRequests/<%= request.id %>"
                                                class="btn btn-outline-light btn-sm">
                                                Details
                                            </a>

                                            <% if (isAdmin) { %>

                                                <a
                                                    href="/helpRequests/<%= request.id %>/edit"
                                                    class="btn btn-outline-warning btn-sm">
                                                    Edit
                                                </a>

                                            <% } %>

                                        </div>

                                    </td>

                                </tr>

                            <% }) %>

                        </tbody>

                    </table>

                </div>

            <% } %>

        </div>

    </div>

</div>